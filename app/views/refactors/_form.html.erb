<div id="editor">function foo(items) {
    var x = "All this is syntax highlighted";
    return x;
}</div>

<%= form_for(@refactor) do |f| %>
	<%= f.hidden_field :code, id:"extract-code" %>
	<%= f.button "Submit" , class: "btn btn-primary", id:"submit" %>  
	

<% end %>
